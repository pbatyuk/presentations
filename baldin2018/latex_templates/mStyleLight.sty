
\ProvidesPackage{mStyleLight}[2016/04/05 Style file for Beamer presentation]

\RequirePackage{lmodern}
\RequirePackage[T2A]{fontenc}
\RequirePackage[utf8]{inputenc}
\RequirePackage[english,russian]{babel}
\RequirePackage{amssymb,amsfonts,amsmath,mathtext,cite,enumerate,float}
\RequirePackage{multirow}
\RequirePackage{graphicx}
\RequirePackage{graphics}
\RequirePackage{multirow}
\RequirePackage{url}
\RequirePackage{hyperref}
\RequirePackage{appendixnumberbeamer} 
\RequirePackage{pgfpages}
\RequirePackage{listings}
\RequirePackage{color}
\RequirePackage{fancybox}
\RequirePackage{animate}
\RequirePackage{ragged2e} %выравнивание текста по ширине слайда (\justifying)


\usetheme{Warsaw}
\usecolortheme{seahorse}

\colorlet{col1}{White!85!MidnightBlue}
\colorlet{col2}{Black!30!MidnightBlue}
\colorlet{col3}{White!98!MidnightBlue}
\colorlet{col4}{Black!10!Red}
\colorlet{col5}{Black!70!Green}

\setbeamercolor{background canvas}{bg=col3}

%=================================================

\setbeamercolor{section in head/foot}{fg=col2, bg=col1}

\defbeamertemplate*{footline}{mytheme}{%
  \leavevmode%
  \hbox{%
    \begin{beamercolorbox}[wd=.3\paperwidth,ht=3ex,dp=1ex,center]{section in head/foot}%
      \usebeamerfont{author in head/foot}\textbf{\insertshortauthor}
    \end{beamercolorbox}%
    \begin{beamercolorbox}[wd=.5\paperwidth,ht=3ex,dp=1ex,center]{section in head/foot}%
      \usebeamerfont{title in head/foot}\textbf{\insertshorttitle}
    \end{beamercolorbox}%
    \begin{beamercolorbox}[wd=.2\paperwidth,ht=3ex,dp=1ex,right]{section in head/foot}%
      %\usebeamerfont{date in head/foot}\insertshortdate{}\hspace*{2em}
      %\insertframenumber{} / \inserttotalframenumber\hspace*{2ex} %номер текущего слайда / общее число слайдов
      \textbf{\insertframenumber{} / \inserttotalframenumber} \hspace*{5ex}  %номер текущего слайда
  \end{beamercolorbox}}%
  \vskip0pt%
}
\usebeamertemplate{mytheme}


\defbeamertemplate*{frametitle}{boldTitle}{%
  \begin{beamercolorbox}[wd=\paperwidth,ht=3ex,dp=2pt,center]{section in head/foot}%
    %        \ \textit{\textbf{\insertframetitle}} % курсивный заголовок слайда 
    \ {\textbf{\LARGE\insertframetitle}}
  \end{beamercolorbox}
}
\usebeamertemplate{boldTitle}

%=================================================

\hyphenpenalty=100000  %убираем переносы в тексте
%\setbeamerfont{normal text){}
\setbeamertemplate{navigation symbols}{} %выключаем навигационные кнопки
\setbeamercolor{normal text}{fg=col2}
\setbeamercolor{alerted text}{fg=col4}
\setbeamercolor{example text}{fg=col5}
\setbeamercolor{date}{fg=col2}
%\setbeamercolor{author}{fg=col4}
\setbeamercolor{title}{fg=col2, bg=col1}

\setbeamercovered{dynamic}

% These slides also contain speaker notes. You can print just the slides, just the notes, or both, depending on the setting below. Comment out the want you want.

\setbeameroption{hide notes} % Only slides
%\setbeameroption{show only notes} % Only notes
%\setbeameroption{show notes on second screen=right} % Both
%\setbeamertemplate{note page}{\pagecolor{yellow!15}\insertnote}

\endinput